1. Insert data into users table

Solution:
INSERT INTO users (first_name, last_name)
VALUES 
('Khaled', 'Esam'),
('Bilal', 'Qanoa'),
('Belal', 'Alshareef'),
('Salah', 'Ahmed'),
('Ahmed', 'Harara'),
('Osama', 'Mosab');

2. Insert into friendship table

Solution:
-- User 1 (Khaled) friends with 2, 4, 6
INSERT INTO friendships (user_id, friend_id)
VALUES (1, 2), (1, 4), (1, 6);

-- User 2 (Bilal) friends with 1, 3, 5
INSERT INTO friendships (user_id, friend_id)
VALUES (2, 1), (2, 3), (2, 5);

-- User 3 (Belal) friends with 2, 5
INSERT INTO friendships (user_id, friend_id)
VALUES (3, 2), (3, 5);

-- User 4 (Salah) friends with 3
INSERT INTO friendships (user_id, friend_id)
VALUES (4, 3);

-- User 5 (Ahmed) friends with 1, 6
INSERT INTO friendships (user_id, friend_id)
VALUES (5, 1);

-- User 6 (Osama) friends with 2, 3
INSERT INTO friendships (user_id, friend_id)
VALUES (6, 2), (6, 3);

3. Display All Relationships

Solution:
SELECT 
    users.first_name, 
    users.last_name, 
    user2.first_name AS friend_first_name, 
    user2.last_name AS friend_last_name
FROM users
JOIN friendships ON users.id = friendships.user_id
LEFT JOIN users AS user2 ON friendships.friend_id = user2.id;

4. Return all users who are friends with the first user

Solution:
SELECT 
    user2.first_name AS friend_first_name, 
    user2.last_name AS friend_last_name
FROM users
JOIN friendships ON users.id = friendships.user_id
LEFT JOIN users AS user2 ON friendships.friend_id = user2.id
WHERE users.id = 1;

5. Return all users who are friends with the first user

Solution:
SELECT 
    user2.first_name AS friend_first_name, 
    user2.last_name AS friend_last_name
FROM users
JOIN friendships ON users.id = friendships.user_id
JOIN users AS user2 ON friendships.friend_id = user2.id
WHERE users.id = 1;

6. Return the count of all friendships

Solution:
SELECT COUNT(*) AS total_friendships FROM friendships;

7. Find out who has the most friends

Solution:
SELECT 
    users.first_name, 
    users.last_name, 
    COUNT(friendships.id) AS total_friends
FROM users
JOIN friendships ON users.id = friendships.user_id
GROUP BY users.id
ORDER BY total_friends DESC
LIMIT 1;

8. Return the friends of the third user in alphabetical order

Solution:
SELECT 
    user2.first_name AS friend_first_name, 
    user2.last_name AS friend_last_name
FROM users
JOIN friendships ON users.id = friendships.user_id
LEFT JOIN users AS user2 ON friendships.friend_id = user2.id
WHERE users.id = 3
ORDER BY friend_first_name ASC;
